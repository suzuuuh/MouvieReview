<p id="notice"></p>
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    	<%= form_with model: Post.new, url: post_path do |f| %>

    	  <article>
          <h2 class="text-center m-5 border-bottom p-1 color:blue;"><%= @movie.name %></h2>

        </div>
        <div style="margin:5px;font-weight: bold; font-size:1em; text-align:center;"><%= @movie.introduction %></div>
      </div>








			<div class="form-group">
         <%= f.submit "レビューを投稿する", class:"btn btn-secondary btn-sm" %>
      </div>
			<% end %>

	   </div>
	   </div>
	   </div>
	   </div>
