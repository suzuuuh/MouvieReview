<p id="notice"></p>

 <div class="border col-8 mx-auto"><br>
	<div class="search_form container text-center">
    <%= form_with url: search_path, local: true, method: :get do |f| %>
    <label for="name" class="col-md-2 control-label">作品名</label>
      <%= f.text_field :word %>
      <%= f.submit "検索", class: "btn btn-sm btn-outline-secondary" %>
    <% end %>
    <% Genre.all.each do |genre| %>
      <%= link_to genre_path(genre.id) do %>
        <%= genre.name %>
      <% end %>
    <% end %>
  </div><br>
  </div>

<p id="notice"></p>
<div class="container" style="text-align: center;">
  <section class="d-flex" style="flex-direction: column;">
	<p id="notice"></p>
      <h3>作品一覧(全<%= Movie.count %>件）</h3><br>
      <div class="d-flex flex-wrap mx-auto">
        <% @movies.each do |movie| %>
          <div class="test" style="width: 180px; margin-top: 2%;">
            <%= link_to movie_path(movie.id) do %>
            <%#= movie_tag movie.get_image, size: "100x100" %>
            <div class="col-8 p-0 m-auto text-decoration-none btn-outline-dark">
              <p><%= movie.name %></p>
              <p><%= movie.introduction %></p>
            </div>
            <% end %>
          </div>
            <% end %>
      </div>
    </section>
  </div>


      <%#= paginate @movies %>
